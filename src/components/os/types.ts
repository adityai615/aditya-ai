export type WindowType =
  | "agent"
  | "projects"
  | "terminal"
  | "about"
  | "resume"
  | "calculator"
  | "settings";

export type WallpaperMeta = {
  id: string;
  name: string;
  path: string;
};
